services:
  energy-market-data:
    build: .
    container_name: energy-market-data
    restart: unless-stopped
    tty: true
    volumes:
      - ./Configuration/example/config.yaml:/app/config.yaml
